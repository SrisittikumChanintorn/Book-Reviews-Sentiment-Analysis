# Book-Reviews-Sentiment-Analysis
This project analyzes book reviews using VADER and DistilBERT models. VADER provides rule-based categorization (positive/neutral/negative), while DistilBERT offers binary classification. Results show VADER excels with expressions, transformers with context. Both methods are visualized with charts.
